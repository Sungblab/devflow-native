# Condition A: No Handoff

Task: Add a `--json` flag to the status command so agent hosts can consume
stable output.

Frozen snapshot:

- Task id: `task-001`
- Repo fixture: `fixtures/repos/minimal-node-cli`
- Snapshot label: `task-001-interrupted-status-json`
- Session 2 starts from the interrupted filesystem and git state.

No prior changed-file list, gate result, blocker note, or suggested continuation
step is included for this condition.
