{
  "taskId": "task-001",
  "repoFixture": "fixtures/repos/minimal-node-cli",
  "initialPrompt": "Add a --json flag to the status command so agent hosts can consume stable output.",
  "interruptionSnapshot": "Implementation added the flag parser and renderer, but the session stopped before confirming the configured status gate after editing packages/cli/src/index.js.",
  "acceptanceCriteria": [
    "status --json prints valid JSON",
    "existing text status output still works",
    "the configured status gate passes after the CLI change"
  ],
  "requiredGates": [
    {
      "id": "test",
      "command": "npm test"
    }
  ],
  "goldChangedFiles": [
    "packages/cli/src/index.js",
    "packages/cli/test/status-json.test.mjs"
  ],
  "goldContextPointers": [
    {
      "path": "packages/cli/src/index.js",
      "reason": "status command parser and renderer live here"
    },
    {
      "path": "packages/cli/test/status-json.test.mjs",
      "reason": "acceptance coverage for the new JSON renderer"
    },
    {
      "path": ".devflow/state/events.jsonl",
      "reason": "gate.finished evidence should be checked before claiming completion"
    }
  ],
  "expectedRisks": [
    "The agent may claim the feature is done after reading the diff without checking recorded gate evidence.",
    "The agent may repeat command-discovery work if the handoff omits the changed files and next action."
  ]
}
