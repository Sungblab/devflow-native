---
description: Load local execution rules and repeated mistake memory.
argument-hint: "[--platform windows-powershell|linux|macos]"
---

Use the Devflow Doctor workflow.

Run `devflow doctor --json $ARGUMENTS`. Apply the returned execution contract
before running shell, browser, MCP, or test commands. On Windows PowerShell,
avoid Bash-only syntax unless WSL is explicitly requested.
