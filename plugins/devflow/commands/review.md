---
description: Request or record Devflow review evidence.
argument-hint: "request|record --work <id> [extra args]"
---

Use the Devflow Review workflow.

Run `devflow status --json`. For review requests, run `devflow review request
$ARGUMENTS`. For recording review, run `devflow review record $ARGUMENTS
--json`. Re-check finish blockers after recording review evidence.
