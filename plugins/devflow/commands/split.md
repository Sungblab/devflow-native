---
description: Plan safe parallel Devflow work sessions.
argument-hint: "[--goal <text>] [--sessions <n>]"
---

Use the Devflow Split workflow.

Run `devflow doctor --json`, then `devflow split $ARGUMENTS --json`. Report
owned paths, avoid paths, worktree commands, verification gates, merge order,
and copy-paste prompts.
